# Pin Assignment Log

Sample select-hw pin assignment log.

The sample uses the selected board definition from `upy-analyze-plugin/boards` and records pin sources:

- `default_bus` for board default bus pins.
- `auto_assigned` for free GPIO selected by the workflow.
- `user_wiring` for user-provided wiring that passed board checks.
- `power` for power rails.
