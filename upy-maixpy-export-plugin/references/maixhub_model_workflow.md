# MaixHub Model Workflow

Status: link_reference

This Skill does not call MaixHub APIs, train models, or download model files. It may only tell the user to prepare a MaixPy model and copy it to a documented path such as `/root/models/model_xxx.mud`.

